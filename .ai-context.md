# Code Style

- Create a separate file for each top level member, like a class, struct, or enum for instance, each should have it's own file. Any violations to this rule should be proactively broken up into separate files.
